# ─────────────────────── Data Collection ───────────────────────
praw==7.7.1                  # Reddit API wrapper
requests==2.31.0             # HTTP requests
feedparser==6.0.11           # RSS/Atom feed parser (rss_scraper)
beautifulsoup4==4.12.3       # HTML parsing for custom scrapers

# ─────────────────────── Streaming ───────────────────────
kafka-python-ng              # Kafka producer/consumer (supports Python 3.12+)

# ─────────────────────── Storage ───────────────────────
pymongo>=4.6,<5.0            # MongoDB driver (pre-built wheel available)

# ─────────────────────── NLP & Feature Engineering ───────────────────────
nltk==3.8.1                  # Natural language toolkit
scikit-learn>=1.5,<2.0       # TF-IDF, ML models (use latest with wheels)
textblob==0.18.0             # Sentiment analysis

# ─────────────────────── Visualization & Dashboard ───────────────────────
streamlit>=1.32,<2.0         # Real-time dashboard
plotly>=5.20,<6.0            # Interactive charts
pandas>=2.2,<3.0             # Data manipulation

# ─────────────────────── Utilities ───────────────────────
python-dotenv==1.0.1         # Load .env files
loguru==0.7.2                # Structured logging
schedule==1.2.1              # Task scheduling
tenacity==8.2.3              # Retry logic for network requests
